
const multiplica = (a: number, b: number): number => {
    return (a*b);
}

const receberNome = (nome: string): string => {
    return ("Olá "+ nome)
}

console.log(multiplica(3,9));
console.log(receberNome("Bruno"));